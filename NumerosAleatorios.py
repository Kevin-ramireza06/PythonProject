import random

azar = random.randint(1,6)

"""
de esta forma generamos numeros alatorios, primero debemos de integrar la libreria de random(Como en java) esta tiene
un metodo randint, donde nos devuelve un entero entre los 2 numeros que le pasemos por parametro, con ambos incluidos
"""

print(azar)